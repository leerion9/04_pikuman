# lib/ 폴더 안내

Flutter 앱의 모든 Dart 소스 코드가 들어있는 루트 폴더입니다.

## 하위 폴더 구조

| 폴더 | 역할 |
|------|------|
| `app/` | 앱 전체 설정 (라우팅, 초기 바인딩) |
| `core/` | 여러 화면에서 공통으로 쓰이는 핵심 기능 |
| `features/` | 각 화면(기능) 단위로 분리된 UI·로직 코드 |
