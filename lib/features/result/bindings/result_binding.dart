// 게임 결과 화면 의존성 주입 파일 - 결과 화면에서 필요한 컨트롤러를 등록하는 파일
import 'package:get/get.dart';

/// 결과 화면 진입 시 필요한 의존성을 등록합니다.
class ResultBinding extends Bindings {
  @override
  void dependencies() {
    // TODO: Phase 5에서 ResultController 등록
  }
}
