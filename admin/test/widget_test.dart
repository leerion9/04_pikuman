// 어드민 앱 기본 위젯 테스트 파일
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Admin app smoke test', (WidgetTester tester) async {
    // 어드민 도구 기본 실행 테스트 (필요 시 확장)
    expect(true, isTrue);
  });
}
